let addButton = document.getElementById("itemadd").id;
let removeButton = document.getElementById("itemremove").id;

console.log(addButton, removeButton);

// Function to add and remove items from Product bar. And calculate the price.
const addRemove = () => {
    
}